package de.secretcraft.scclient.event;

/**
 * All Events must be subclasses of <code>BaseEvent</code>. Otherwise the
 * {@link EventManager} will fail to bind the events correctly.
 */
public abstract class BaseEvent {
	
}
